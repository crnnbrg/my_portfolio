<p class="white-text">
<span class="glyphicon glyphicon-user"></span>
<%= comment.content %>
<%= comment.user.email%>
<%if comment.user == current_user%><%= link_to '(remove)', [comment.post, comment] , method: :delete, data: {confirm: 'Are you sure'}, remote: true, class: 'del_comment' %>
<%else%>
<h1>No comments yet!</h1>
<%end%>
</p>
